import 'package:flutter/material.dart';

// Function to change the widget tree dynamically
Widget changeWidgetBodyTree(BuildContext context, Widget newContent) {
  // Returns the new widget tree content
  return newContent;
}
